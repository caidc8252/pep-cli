{
  "name": "@newlandnpt/pep-cli",
  "version": "0.1.3",
  "description": "PEP CLI —— 凭 PEP 身份读取 Newland 开发者文档与 agent skills",
  "license": "UNLICENSED",
  "type": "module",
  "os": [
    "win32",
    "darwin"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.18.3",
  "bin": {
    "pep": "dist/pep.cjs",
    "pep-cli": "dist/pep.cjs"
  },
  "files": [
    "dist/pep.cjs",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/build-js.mjs",
    "build:exe": "node scripts/build-exe.mjs production",
    "build:exe:dev": "node scripts/build-exe.mjs development",
    "build:exe:prod": "node scripts/build-exe.mjs production",
    "build:installer": "node scripts/build-installer.mjs production",
    "build:installer:dev": "node scripts/build-installer.mjs development",
    "build:installer:prod": "node scripts/build-installer.mjs production",
    "prepack": "node scripts/build-js.mjs production",
    "build:exe:view": "node scripts/build-exe.mjs view",
    "build:installer:view": "node scripts/build-installer.mjs view"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "postject": "^1.0.0-alpha.6",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
